# 配置protoc生成规则
version: v1

managed:
  enabled: true
  optimize_for: SPEED

  go_package_prefix:
    default: github.com/chnxq/x-curd/api/gen
    except:
      - 'buf.build/googleapis/googleapis'
      - 'buf.build/gnostic/gnostic'

plugins:
  # generate go struct code
  #- plugin: buf.build/protocolbuffers/go
  - name: go
    out: gen
    opt: paths=source_relative